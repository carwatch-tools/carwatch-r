% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/results.R
\name{new_carwatch_results}
\alias{new_carwatch_results}
\title{Construct canonical CARWatch results}
\usage{
new_carwatch_results(data, column_spec, display_only = FALSE)
}
\arguments{
\item{data}{Tibble containing \code{participant} and one column per result value.}

\item{column_spec}{Tibble with \code{name}, \code{day}, \code{sample}, and \code{variable}.}

\item{display_only}{Whether required provenance has intentionally been removed.}
}
\value{
A \code{carwatch_results} object: a tibble with one row per participant,
a \code{column_spec} attribute mapping value columns to day, sample, and
variable, and a \code{carwatch_display_only} attribute.
}
\description{
A \code{carwatch_results} object is an R-native representation of the Python
package's participant-indexed, three-level wide table. Internally it is a
tibble with a \code{participant} column and safe column names. The \code{column_spec}
attribute maps every value column reversibly to \code{day}, \code{sample}, and
\code{variable}; opaque sample IDs are never encoded into the internal names.
}
