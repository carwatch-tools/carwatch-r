# Python parity fixtures

The v1.0.0 fixtures are generated by tools/generate_python_fixtures.py from
the adjacent carwatch-python checkout. They are versioned development
artifacts, not package runtime data. Regenerate them only with
carwatch-python 1.0.0 and review semantic CSV/report changes before commit.
